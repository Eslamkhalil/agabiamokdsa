
import 'package:flutter/material.dart';

class EndPray extends StatelessWidget {
 // const EndPray({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('end pray',style: TextStyle(
        fontSize: 40.0,
        color: Colors.white
      ),),
    );
  }
}
